<?php



	//This variable represents the absolute path of where our logs files are stored via string.  Log files include the original file before modification, as well as the difference between the modified
	//file and the new file
	$locationOfLogFiles = realpath(__DIR__)."/"."logs";
	
	//This variable holds the absolute path in string form of the where the OCR files that will be crowdsourced.  This variable preferably takes the format /path/to/directory  with no / at the end 
	$locationOfAllOcrFiles = (file_get_contents('constants/ocrFilesPath.txt'));

?>